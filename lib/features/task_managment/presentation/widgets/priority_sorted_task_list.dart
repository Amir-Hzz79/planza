import 'package:flutter/material.dart';

class PrioritySortedTaskList extends StatelessWidget {
  const PrioritySortedTaskList({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('PrioritySortedTaskList');
  }
}
